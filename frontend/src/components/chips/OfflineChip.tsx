import { Chip } from "@mui/material";

export default function OfflineChip() {
  return (
    <Chip
      size="small"
      label="Offline-ready"
      color="success"
      variant="outlined"
    />
  );
}
